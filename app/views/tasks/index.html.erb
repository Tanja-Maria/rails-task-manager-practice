<h1>My tasks</h1>

<ol>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %> <p> <%= link_to "Delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </p></li>

  <% end %>
</ol>

<%= link_to "Add a new task", tasks_new_path %>
